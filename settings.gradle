pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

// ForgeGradle 5.1 adds its own bundled repository while applying the plugin.
// Do not use FAIL_ON_PROJECT_REPOS here; it conflicts with that repository.
rootProject.name = 'machine-defence'
